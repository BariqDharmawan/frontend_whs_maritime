import { Icon } from "@iconify-icon/react";

const IconNestedDropdown = ({ className = "className" }) => {
	return <Icon icon="mingcute:left-fill" width="24" className={className} height="24" />;
};

export default IconNestedDropdown;
